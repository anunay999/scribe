---
description: Scan the current conversation for wiki-worthy content and propose what to save — decisions, facts, gotchas, new concepts, sources.
---

Invoke the `scribe:capture` skill via the Skill tool. Follow the skill's process exactly: scan recent turns, propose a sectioned checklist, wait for the user to pick which to save, apply selections, log.

Optional scope hint (e.g. "since the last commit", "today's debugging session"): $ARGUMENTS
