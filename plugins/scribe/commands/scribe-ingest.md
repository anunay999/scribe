---
description: Ingest a source (URL, file, paste) into the wiki — read, summarize, file under sources/, propagate to affected pages, append to log.
---

Invoke the `scribe:ingest` skill via the Skill tool, passing the user's input as the source identifier. Follow the skill's instructions exactly. End with the discussion message the skill describes.

Source: $ARGUMENTS
