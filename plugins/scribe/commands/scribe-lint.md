---
description: Health-check the wiki — orphan pages, contradictions, stale claims, missing cross-references, undocumented concepts.
---

Invoke the `scribe:lint` skill via the Skill tool. Follow the skill's seven checks. Report findings in the structured format the skill prescribes. Only auto-fix sections the user explicitly names.

Scope (optional — leave blank for full vault): $ARGUMENTS
