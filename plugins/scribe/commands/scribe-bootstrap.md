---
description: Bootstrap a new LLM-maintained wiki under ~/Documents/obsidian/<vault>/. One-time setup.
---

Invoke the `scribe:bootstrap` skill via the Skill tool, passing the user's input as args. Follow the skill's instructions exactly. Report the resulting vault tree and the next-step commands to the user.

User input: $ARGUMENTS
